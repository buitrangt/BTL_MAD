<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BudgetTracker" parent="Base.Theme.BudgetTracker" />
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
    <style name="OtpEditTextStyle">
        <item name="android:hint">0</item>
        <item name="android:textColorHint">#D1D5DB</item> <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">55dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:background">@drawable/bg_edittext_gray</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#1F2937</item>

        <item name="android:importantForAutofill">no</item>
    </style>

    <!-- Base style for ExpenseTracker components -->
    <style name="ExpenseTracker" />

    <!-- Base style for SelectDate components -->
    <style name="SelectDate" />
</resources>